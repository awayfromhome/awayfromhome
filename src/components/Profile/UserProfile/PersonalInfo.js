import React from 'react';
import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles(theme => ({}));

const AcountInfo = () => {
  const classes = useStyles();

  return <div>AcountInfo</div>;
};

export default AcountInfo;
